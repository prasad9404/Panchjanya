{
    "name": "panchjanya-functions",
    "version": "1.0.0",
    "description": "Firebase Cloud Functions for Panchjanya platform",
    "main": "lib/index.js",
    "scripts": {
        "build": "tsc",
        "build:watch": "tsc --watch",
        "serve": "npm run build && firebase emulators:start --only functions",
        "deploy": "npm run build && firebase deploy --only functions",
        "logs": "firebase functions:log"
    },
    "engines": {
        "node": "20"
    },
    "dependencies": {
        "firebase-admin": "^12.0.0",
        "firebase-functions": "^5.0.0",
        "sharp": "^0.33.4"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "typescript": "^5.4.5"
    },
    "private": true
}